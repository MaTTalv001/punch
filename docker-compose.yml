version: "3"
services:
  app:
    build: .
    image: interview_simulator
    ports:
      - "3000:3000"
    volumes:
      - ".:/app"
      - "/app/node_modules"
    environment:
      - NODE_ENV=development
networks:
  supabase_default:
    external: true